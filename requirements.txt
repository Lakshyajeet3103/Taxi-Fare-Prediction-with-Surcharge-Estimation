# Versions pinned to the environment the committed results were produced in (Python 3.13.5).
numpy==2.2.6
pandas==2.3.0
scikit-learn==1.7.2
scipy==1.16.0
joblib==1.5.2
matplotlib==3.10.3
pyarrow==21.0.0
# Notebook kernel (taxi_fare.ipynb); install `notebook` or `jupyterlab` separately if you want a browser UI.
ipykernel==6.29.5
